import { Box } from "@mui/material"

export const AdminPage = ()=> {
    return (
      <p style={{backgroundColor:'blue'}}>
              We need here forms for adding category and product
            </p>
    )
}